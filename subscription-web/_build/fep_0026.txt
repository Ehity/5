handlers = '  async function handleGenerate() {' + NL + '    setGenerating(true);' + NL + '    try {' + NL + '      const d = await generateTest();' + NL + '      setPreview(d);' + NL + '    } finally {' + NL + '      setGenerating(false);' + NL + '    }' + NL + '  }' + NL + '  async function handleScanGenerated() {' + NL + '    setScanning(true);' + NL + '    try {' + NL + '      const d = await scanGenerated(preview.csv);' + NL + '      setData(d);' + NL + '      setCancelled([]);' + NL + '      setPreview(null);' + NL + '    } finally {' + NL + '      setScanning(false);' + NL + '    }' + NL + '  }' 
